# Build-time configuration ONLY. Never put secrets here (per DECISIONS D4):
# the GitHub PAT, Ollama URL, and model are runtime settings entered in-app.

# Base path for the static build. GitHub Pages project sites are served under
# "/<repo>/". Set to "/" for a root deploy or a custom domain.
BASE_PATH=/tether/
